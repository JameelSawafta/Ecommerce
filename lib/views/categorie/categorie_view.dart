import 'package:flutter/material.dart';

class CategorieView extends StatelessWidget {
  const CategorieView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Categorie View'),
      ),
    );
  }
}
